{
  "name": "autobrowser",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "autobrowser": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "dev": "bun src/cli.ts server",
    "start": "bun src/cli.ts server",
    "test": "bun test",
    "fmt": "oxfmt src/ extension/",
    "lint": "oxlint src/ extension/",
    "fix": "oxfmt --fix src/ extension/ && oxlint --fix src/ extension/",
    "typecheck": "bun scripts/run-typescript-compiler.ts --noEmit && bun scripts/run-typescript-compiler.ts -p extension/tsconfig.json",
    "typecheck:native": "tsgo --noEmit && tsgo -p extension/tsconfig.json",
    "typecheck:tsc": "tsc --noEmit && tsc -p extension/tsconfig.json",
    "check": "bun run typecheck && oxlint src/ extension/ && oxfmt src/ extension/",
    "build:cli": "bun scripts/build-cli.ts",
    "build:chrome": "bun scripts/build-chrome.ts",
    "build": "bun run build:cli && bun run build:chrome",
    "link:bun": "bun scripts/link-bun.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "@types/node": "^25.6.0",
    "@typescript/native-preview": "beta",
    "bun-types": "^1.3.12",
    "husky": "^9.1.7",
    "typescript": "^6.0.2"
  },
  "engines": {
    "bun": ">=1.3.12"
  }
}
