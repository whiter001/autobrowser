{
  "name": "autobrowser",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "autobrowser": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "dev": "bun src/cli.ts server",
    "start": "bun src/cli.ts server",
    "test": "bun test",
    "fmt": "oxfmt src/ extension/ scripts/ test/",
    "lint": "oxlint src/ extension/ scripts/ test/",
    "fix": "oxfmt --fix src/ extension/ scripts/ test/ && oxlint --fix src/ extension/ scripts/ test/",
    "typecheck": "tsc --noEmit && tsc -p extension/tsconfig.json && tsc -p extension/background/tsconfig.json && tsc -p tsconfig.tools.json",
    "check": "bun run typecheck && oxlint src/ extension/ scripts/ test/ && oxfmt src/ extension/ scripts/ test/",
    "build:cli": "bun scripts/build-cli.ts",
    "build:chrome": "bun scripts/build-chrome.ts",
    "build": "bun run build:cli && bun run build:chrome",
    "link:bun": "bun scripts/link-bun.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/chrome": "^0.1.42",
    "@types/node": "^24.12.4",
    "husky": "^9.1.7",
    "oxfmt": "0.15.0",
    "oxlint": "1.30.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "bun": ">=1.3.13"
  }
}
